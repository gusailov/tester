<% if resource.errors.present? %>
  <div>

    <h3> <%= t("detected") %>: <%= pluralize(resource.errors.count, "error") %> </h3>
    
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %> </li>
      <% end %>
    </ul>  

  <div>
 
<% end %>
