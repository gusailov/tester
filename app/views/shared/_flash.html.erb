<% flash.each do |key, message| %>
  <% k = { "alert" => "danger", "notice" => "success" } %>
  <% class_ad = k[key] || "secondary" %>
  <%= content_tag :p, sanitize(message), class: "alert alert-#{class_ad} mt-2" %> 
<% end %> 
