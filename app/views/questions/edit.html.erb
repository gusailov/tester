<%= question_header(@question) %> 

<%= render "form" %>

<%= link_to "Add Answer", new_question_answer_path(@question) %> 

<h4>Answers:</h4>
  <ul>
    <%= render @question.answers %>
  </ul>

<%= link_to "back to test", test_path(@question.test_id) %>
